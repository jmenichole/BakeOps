// Shared types barrel — re-exports all database types for convenient single-import access
export type {
  CakeConfig,
  CakeDesign,
  Order,
  Baker,
  PrepTask,
  AnalyticsEvent,
} from './database';
