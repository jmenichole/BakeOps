{
  "name": "bakeops-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build:app": "npm run build -w app",
    "build": "npm run build:app",
    "dev:app": "npm run dev -w app",
    "lint:app": "npm run lint -w app",
    "lint": "npm run lint:app",
    "install:all": "npm install",
    "prepare": "husky"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "date-fns": "^4.1.0",
    "resend": "^6.9.2"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "supabase": "^2.76.4"
  }
}
