{
  "name": "bakeops-monorepo",
  "version": "1.0.0",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build:app": "npm run build -w app",
    "build": "npm run build:app",
    "dev:app": "npm run dev -w app",
    "lint:app": "npm run lint -w app",
    "lint": "npm run lint:app",
    "install:all": "npm install",
    "prepare": "node -e \"if (process.env.CI || process.env.VERCEL) { process.exit(0); } else { require('child_process').execSync('husky', {stdio: 'inherit'}); }\""
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "supabase": "^2.84.8"
  }
}
